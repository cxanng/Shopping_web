describe("Set product as favourite", () => {
  it("The star icon should be yellow when user click first time", () => {
    throw "Not implemented";
  })

  it("The favourite item should appear in favourite tab", () => {
    throw "Not implemented";
  })
});

describe("Search toolbar", () => {
  it("Should show products with filtered name", () => {
    throw "Not implemented";
  });

  it("Should show no products when the filter is invalid", () => {
    throw "Not implemented";
  });

  it("Show all products when the bar is empty", () => {
    throw "Not implemented";
  });
});

describe("Report creation", () => {
  it("The report form should contain necessary fields", () => {
    throw "Not implemented";
  });

  it("The report should be visible to admin accounts, but not customers", () => {
    throw "Not implemented";
  });

  it("Should show notification when report is created", () => {
    throw "Not implemented";
  });
});